version: 2
machine:
  services:
    - docker

test:
  override:
    - docker-compose run editor yarn lint:javascript
    - docker-compose run editor yarn lint:stylus
    - docker-compose run editor yarn unit
  post:
    - docker-compose run editor yarn coverage
