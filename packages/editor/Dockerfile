# Setup Yarn Editor Configuration
FROM node:7.10

# Create Working Directory
RUN mkdir -p /home/app

# Change to working directory
WORKDIR /home/app

# Copy Source Over to Docker Container
COPY . /home/app

# Start the App
# RUN yarn start
